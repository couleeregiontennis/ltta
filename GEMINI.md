# LTTA Project Context

## Workflow & Conventions
- **Staging-First Promotion Flow**: All changes MUST go through the `staging` branch first. Never merge directly into `main` or target `main` for feature/fix PRs. The promotion flow is always: Feature Branch -> Staging -> Main.
